(function () { if (localStorage.getItem('rb_cookie_notice_ack')) return; var el = document.createElement('div'); el.className = 'cookie-banner'; el.innerHTML = '<p>readerbull doesn\'t use tracking or advertising cookies. We only use your browser\'s local storage to keep you signed in, nothing else. See our <a href="privacy.html">Privacy Policy</a> for details.</p><div class="cookie-banner-actions"><button class="btn btn-primary btn-sm" id="cookie-ack-btn">Got it</button></div>'; document.body.appendChild(el); requestAnimationFrame(function () { el.classList.add('show'); }); document.getElementById('cookie-ack-btn').addEventListener('click', function () { localStorage.setItem('rb_cookie_notice_ack', '1'); el.classList.remove('show'); setTimeout(function () { el.remove(); }, 200); }); })();
