module.exports = async function handler(req, res) { if ((req.headers['x-backup-secret'] || '') !== process.env.BACKUP_SECRET) { res.status(401).json({ error: 'unauthorized' }); return; } var key = process.env.RESEND_API_KEY; if (!key) { res.status(500).json({ error: 'RESEND_API_KEY missing' }); return; } var r = await fetch('https://api.resend.com/emails', { method: 'POST', headers: { 'Authorization': 'Bearer ' + key, 'Content-Type': 'application/json' }, body: JSON.stringify({ from: 'Readerbull Alerts <alerts@readerbull.com>', to: ['coastlvibes@gmail.com'], subject: 'Readerbull error: test-alert', text: 'This is a one-off test of the error alert wiring, triggered manually to confirm delivery. Safe to ignore, this endpoint will be deleted immediately after.' }) }); var body = await r.text(); res.status(r.ok ? 200 : 502).json({ ok: r.ok, status: r.status, body: body }); };
